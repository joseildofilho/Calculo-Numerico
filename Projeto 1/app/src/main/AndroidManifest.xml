<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="calcnumerico.ph.mquinabinria">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">
        <activity
            android:name="calcnumerico.ph.maquinabinaria.Activities.MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="calcnumerico.ph.maquinabinaria.Activities.BinaryMachineActivity"
            android:windowSoftInputMode="adjustResize" />
        <activity android:name="calcnumerico.ph.maquinabinaria.Activities.SimulatorActivity" />
        <activity android:name="calcnumerico.ph.maquinabinaria.Activities.BisectionActivity" />
        <activity android:name="calcnumerico.ph.maquinabinaria.Activities.NewtonActivity"></activity>
    </application>

</manifest>